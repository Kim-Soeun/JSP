create table member (
	id varchar(20),
	name varchar(20),
	pw varchar(20),
	pw2 varchar(20),
	phone varchar(20),
	email varchar(20),
	msg varchar(20),
	path varchar(20),
	primary key(id)
);

select * from member;